# Core dependencies for deploy-tool
click>=8.0
rich>=10.0
pyyaml>=6.0
aiofiles>=0.8
python-dateutil>=2.8
jsonschema>=4.0
packaging>=21.0

# Optional dependencies (comment out if not needed)
# bce-python-sdk>=0.8  # For BOS storage support
# boto3>=1.20         # For S3 storage support
# lz4>=4.0            # For LZ4 compression support